apiVersion: v2
name: pingflo-agent-aws
description: Pingflo AWS discovery agent — read-only EC2/RDS/EKS/S3 etc. Outbound HTTPS only.
type: application

# Chart version — bump on every change to chart templates / values
# defaults. SemVer; chart-releaser keys releases on this.
version: 0.1.2

# AWS agent binary version this chart was tested against. Decoupled
# from chart `version` so chart edits + binary cuts can be timed
# independently.
appVersion: "22-05-2026-5"

icon: https://pingflo.ai/logo.png
home: https://pingflo.ai
sources:
  - https://github.com/cloud2scale/pingflo

keywords:
  - pingflo
  - discovery
  - agent
  - aws
  - sre

maintainers:
  - name: cloud2scale
    url: https://pingflo.ai
